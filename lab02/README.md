Лабораторная работа 2
Структуры в языке C
Цель работыИзучить объявление и использование структур, вложенные структуры, указатели на структуры, массивы структур, передачу структур в функции, перечисления (enum) и объединения (union).

Программа представляет собой систему управления данными студентов, реализованную на языке C. В программе демонстрируются:
Работа с перечислениями (enum)
Работа с массивами структур
Cортировка данных
Поиск по критериям
Работа с пользовательским вводом
Основные функции

addStudent() - добавление нового студента

Ввод имени, возраста, курса и среднего балла
Автоматическое добавление в массив
printStudents() - вывод всех студентов

Отображение полной информации о каждом студенте
findByName() - поиск студента по имени

Регистрозависимый поиск
Вывод полной информации найденного студента
sortByAverage() - сортировка по среднему баллу

Сортировка пузырьком (bubble sort)
По убыванию среднего балла
для запуска программы: 
Перейти в папку lab02: cd lab02
Скомпилировать выбранное задание с помощью GCC: gcc n1.c -o n1
Запустить программу: ./n1

Ответы на вопросы: 
Задание 1:
 struct Name {
    тип поле;
};
Задание 2:
Это структура внутри другой структуры (например дата внутри человека).
struct Date { int d, m, y; };
struct Student {
    char name[50];
    struct Date birth;
};
Задание 3: 
по значению: func(struct A a)
по указателю: func(struct A *a)
Задание 4:
Да с помощью struct Student arr[10];
Задание 5:
group[0].age = 20; или group[i].name
Задание 6:
Да 
enum Course { FIRST, SECOND };
struct Student {
    enum Course kurs;
};
Задание 7:
Нет нужно сравнивать каждое поле вручную
if (s1.age == s2.age && s1.avg == s2.avg)
Аркадьев Савва К0709-25/2

